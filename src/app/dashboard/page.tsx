// Phase 0 placeholder so login lands on a real, shell-wrapped page.
// Person A replaces this with the KPI cards, Recent Trips table, and status chart.
export default function DashboardPage() {
  return (
    <div>
      <h1 className="font-comic font-bold text-3xl">Dashboard</h1>
      <p className="font-comic text-[var(--fg-dim)] mt-2">
        Foundation ready. Screens land here as each track ships.
      </p>
    </div>
  );
}
